[project]
name = "sankat-saathi-dataset"
version = "0.1.0"
description = "Dataset and training-readiness pipeline for Sankat Saathi."
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
